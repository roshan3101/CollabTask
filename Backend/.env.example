# Local development – copy to .env and set JWT_SECRET_KEY
# For hybrid (Postgres+Redis in Docker, backend on host): use localhost below.
# For full Docker: docker-compose overrides POSTGRES_HOST/REDIS_HOST for the backend container.

POSTGRES_DB=collabtask
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

JWT_SECRET_KEY=dev-secret-key-change-in-production
ENVIRONMENT=development

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Optional: emails (OTP, invites). Leave empty to skip.
SENDGRID_API_KEY=
FROM_EMAIL=noreply@collabtask.com
FROM_NAME=CollabTask

# Optional: AWS (if used)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=eu-north-1

# CORS (default includes localhost:3000). Add more if needed.
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
